# Required on Vercel. Connect Upstash Redis from the Vercel Marketplace.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
# The integration's KV_REST_API_URL / KV_REST_API_TOKEN also work.
# Without Redis, local development uses .sardines.sqlite3.
