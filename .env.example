# Required on Vercel. Copy the database URL and read/write token from Turso.
# Both libsql://database-organization.turso.io and https:// URLs are accepted.
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
# Without either variable, local development uses .sardines.sqlite3.
# Export variables in your shell; this app does not automatically load .env.
